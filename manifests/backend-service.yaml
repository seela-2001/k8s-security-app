apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: security-app
spec:
  type: ClusterIP
  selector: 
    app: backend
  ports:
    - port: 5678
      targetPort: 5678
      protocol: TCP
